# 历史日志行操作变更记录

## 本次调整
- 删除 `历史日志存档` 表格中每一行的 `下载` 图标按钮。
- 同步删除静态示例行和 `renderTableRows` 动态渲染中的下载按钮。
- 将行内 `删除` 按钮接到现有 `history-delete` 接口。
- 删除成功后在前端即时移除对应行并刷新分页。

## 目的
- 与当前可用能力保持一致，避免用户点击无效下载入口。
- 只做单页最小改动，不影响顶部批量导出按钮和其他页面。
