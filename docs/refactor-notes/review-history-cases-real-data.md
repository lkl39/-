# 人工复核页历史相似案例真实数据替换记录

## 本次修改
- 文件：`public/inner-pages/人工复核/code.html`
- 将“历史相似案例”区域中的两条静态演示文案移除。
- 新增 `history-case-list` 容器，作为历史复核记录的动态挂载点。
- 新增前端渲染逻辑，调用 `/api/inner-data?view=history-cases` 渲染真实历史复核记录。
- 当没有真实数据或请求失败时，显示空态提示，避免继续展示假内容。

## 复用关系
- 直接复用现有 `/api/inner-data` 中的 `view=history-cases` 分支。
- 未新增接口，未修改 `review_cases` 相关字段结构。

## 验收结果
- 页面中不再出现 “2024-03-12 营销活动导致的连接耗尽” 与 “2023-11-05 凌晨报表任务占用过多连接” 两条假案例。
- 历史相似案例区域现在只显示真实复核数据或空态。
