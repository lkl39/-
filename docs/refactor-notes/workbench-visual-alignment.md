# 工作台视觉与交互对齐记录

## 背景
工作台已完成第一轮去 iframe 试点，但新壳和内容区在视觉层次、色值和部分交互上仍与原静态页存在细微差异。

## 本次调整
1. 继续收紧 dashboard 公共壳的容器边框、阴影、留白与色值。
2. 对齐顶部通知弹层交互，补上点击外部关闭行为。
3. 调整工作台卡片、趋势图、最近分析结果、待处理事项的文字色和 hover 层次，使其更接近原静态页。

## 影响范围
- 仅影响 `/dashboard` 工作台试点页。
- 不影响仍使用 `StaticInnerPage` 的其它 dashboard 页面。
- 不改数据字段和 API 契约。
